<!DOCTYPE html>
<html>
<head>
    {{! Document Settings }}
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />

    {{! Page Meta }}
    <title>{{@blog.title}} - {{meta_title}}</title>
    <meta name="description" content="{{meta_description}}" />

    {{! Responsive Meta }}
    <meta name="HandheldFriendly" content="True" />
    <meta name="MobileOptimized" content="320" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1, maximum-scale=1" />

    {{! Styles'n'Scripts }}
    <link rel="shortcut icon" href="{{asset "/favicon.ico"}}">
    <link rel="stylesheet" type="text/css" href="{{asset "css/style.css"}}" />

    <style>
      /*
        Blog Cover Style & Gradient
      */
        {{#if @blog.cover}}
        .aside_satin {
          background:
          -moz-linear-gradient(top, rgba(0, 0, 0, 1),  rgba(0, 0, 0, 0)),
          -moz-linear-gradient(bottom, rgba(0, 0, 0, 1),  rgba(0, 0, 0, 0)),
          url('{{@blog.cover}}');
          background:
          -webkit-linear-gradient(top, rgba(0, 0, 0, 1),  rgba(0, 0, 0, 0)),
          -webkit-linear-gradient(bottom, rgba(0, 0, 0, 1),  rgba(0, 0, 0, 0)),
          url('{{@blog.cover}}');
          background:
          linear-gradient(top, rgba(0, 0, 0, 1),  rgba(0, 0, 0, 0)),
          linear-gradient(bottom, rgba(0, 0, 0, 1),  rgba(0, 0, 0, 0)),
          url('{{@blog.cover}}');
          background-size: auto 100%;
          background-position: 0 0;
          background-repeat: no-repeat;
        }
        @media screen and (max-width: 768px) {
          .aside_satin { background: black; }
        }
        {{/if}}
    </style>

    {{! Ghost outputs important style and meta data with this tag }}
    {{ghost_head}}
</head>

<body class="{{body_class}}">

<div class="wrapper_satin">
   {{{body}}}
</div>

  {{ghost_foot}}
</body>
</html>
